NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Vercel automatically sends this as `Authorization: Bearer <value>` to cron
# routes. Set the same value as Railway's SINAMA_KEEPALIVE_SECRET.
# CRON_SECRET=replace-with-a-long-random-secret
